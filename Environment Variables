printenv or env   #смотреть переменные окружения
echo $HOME   #смотреть конкретную переменную окружения
HISTSIZE=2000   #задать новое временное значение для уже существующей переменной окружения
.bashrc   #если нужно задать особенную переменную окружения для конретного пользователя, т.к. файл лежит в хомяке юзера
export MYVAR=TRUE   #задать новую переменную в файле .bashrc
source .bashrc   #применить новые переменные окружения
sudo vim /etc/environment   #если нужно задать особенную переменную окружения для всех пользователей, тут переменные задаются без слова export
KODEKLOUD=https://kodekloud.com/   #пример
sudo vim /etc/profile.d/lastlogin.sh   #если нужно задать скрипт, который будет выполняться при логине любого пользователя
# в подобных скриптах не нужен shebang, т.к. система уже знает, что его нужно выполнять с помощью bash
sudo vim /etc/skel/README   #skel=skeleton, здесь лежат файлы, которые автоматически копируются в хомяки созданных юзеров
