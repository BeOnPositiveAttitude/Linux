stat file1   #отобразит информацию о файле, в том числе номер inode, количество линков на этот файл
# inode указывает на блоки данных на диске, в которых хранится файл, а также содержит метаданные о файле (дата создания, permissions и т.д.)
ln path_to_target_file path_to_link_file   #создаст hard-link, hard-link указывает на ту же самую inode, что и оригинальный файл
# если удалить оригинальный файл, то открыть этот файл через hard-link все равно возможно, т.к. у inode останется "живая" link на этот файл
# если удалить и сам hard-link, то тогда уже количество link на этот файл у inode будет равняться 0 и данные будут удалены с диска окончательно
# hardlink можно сделать только для файла, но не для директории
ln -s path_to_target_file path_to_link_file   #создать soft-link
readlink shortcut.jpg   #если в выводе ls -l не отображается полный путь soft-link, то можно воспользоваться этой командой
# soft-link можно создать и для файла и для директории
