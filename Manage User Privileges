groups   #смотреть в каких группах состоит текущий юзер
sudo gpasswd -a trinity wheel   #добавить тринити в группу wheel, чтобы дать ей админские права
sudo visudo   #для редактирования файла /etc/sudoers
%wheel ALL=(ALL) ALL   #user/group host=(run_as_user) command_list, % означает группу
sudo -u trinity ls /home/trinity   #запустить команду от лица юзера тринити
sudo -lU user1   #смотреть какие sudo-команды может запускать юзер1

su - rds-server   #при попытке переключиться на пользователя с shell = /sbin/nologin получим сообщение "This account is currently not available."
# Нужно делать это командой: su -s /bin/bash rds-server

root ALL=(ALL:ALL) ALL 
Первое поле показывает имя пользователя, которое правило будет применять к (root).
Первое ALL означает, что данное правило применяется ко всем хостам.
Второе ALL означает, что пользователь root может запускать команды от лица всех пользователей.
Третье ALL означает, что пользователь root может запускать команды от лица всех групп.
Последнее ALL означает, что данные правила применяются всем командам.
