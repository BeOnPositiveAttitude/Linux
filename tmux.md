Разделить терминал вертикально на два: `Ctrl + b` => `Shift + 5 (%)`.

Включить мультиввод: `Ctrl + b` => `:setw synchronize-panes on`. Соответственно выключить `... off`.

Переключаться между терминалами (panes): `Ctrl + b` => `o`.
