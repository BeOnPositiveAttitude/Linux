chmod 4664 file1   #первая цифра 4 означает установить SUID на файл, в данном случае в комбинации со следующей цифрой 6, 
# означающей права только на чтение/запись, мы увидим -rwSrw-r-- file1, таким образом S большая означает права на запуск файла от имени владельца
# с правами только на чтение/запись
chmod 4764 file1   #в данном случае в комбинации с цифрой 7, означающей права на чтение/запись/исполнение, мы увидим -rwsrw-r-- file1, таким образом 
# s маленькая означает права на запуск файла от имени владельца с права на чтение/запись/исполнение
chmod 2664 file2   #все тоже самое аналогично для SGID, цифра 2 означает установить SGID, в данном случае в комбинации со следующей цифрой 6, 
# означающей права только на чтение/запись, мы увидим -rw-rwSr-- file2, таким образом S большая означает права на запуск файла от имени группы-владельца
# с правами только на чтение/запись, аналогично для s маленькой
# можно установить оба и SUID и SGID на один файл с помощью первой шестерки, например 6664
chmod 1777 dir1   #все тоже самое аналогично для sticky bit, цифра 1 означает установить sticky bit, в данном случае в комбинации со следующей цифрой 7,
# означающей права на чтение/запись/исполнение, мы увидим drwxrwxrwt dir1, таким образом t означает, что файл внутри dir1 может быть удален только
# владельцем этого файла или root-ом
find . -perm /4000   #искать в текущей директории файлы с установленным SUID
find . -perm /2000   #искать в текущей директории файлы с установленным SGID
find . -perm /6000   #искать в текущей директории файлы с установленным SUID/SGID
chmod u+s,g+s,o+t /home/bob/datadir   #установить SUID, SGID и sticky bit
